export type PostItem = {
  id: string
  userName: string
  userImageUrl: string
  postText: string
  postImageUrl: string
}

export type Profile = {
  id: string
  userName: string
  userImageUrl: string
}
